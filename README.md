# Out of box version of Elasticsearch version 1.7.1 with plugins: IK, IK-pinyin, head

# To start elasticsearch
./bin/elasticsearch

# Default Analyzer
The default analyzer is ik, in order to support chinese.


